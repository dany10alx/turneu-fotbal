{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Football Tournament API",
            "type": "debugpy",
            "request": "launch",
            "module": "uvicorn",
            "args": [
                "turneu_fotbal.main:app",
                "--reload"
            ],
            "env": {
                "PYTHONPATH": "${workspaceFolder}/src"
            },
            "jinja": true
        }
    ]
}